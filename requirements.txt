# Core Execution & GUI
PySide6>=6.6.0
qasync>=0.27.1

# Asynchronous Networking & Scraping
aiohttp>=3.9.0
nest_asyncio>=1.6.0
beautifulsoup4>=4.12.0
fake-useragent>=1.4.0
requests>=2.31.0
playwright>=1.41.0
cloudscraper>=1.2.71

# DNS, Recon & Cryptography
dnspython>=2.6.0
python-whois>=0.8.0
tldextract>=5.1.0
yarl>=1.9.4

# Offensive Identifiers (Optional/Specific Tooling Bridges if needed by Core)
# wafw00f>=2.2.0
# impacket>=0.11.0

# PyRoxy - Proxy Management Local Instance
# (Can be fetched via git, or handled natively if the src code is embedded)
