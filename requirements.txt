# Nexus Scanner - Python dependencies
# NOTE: the import `dns.resolver` comes from `dnspython`; do NOT install `dns` or legacy `pydns`.

# Core Execution & GUI
PySide6>=6.11.1
qasync>=0.28.0

# Asynchronous Networking & Scraping
aiohttp>=3.14.0
requests>=2.34.2
beautifulsoup4>=4.14.3
lxml>=6.1.1
fake-useragent>=2.2.0
playwright>=1.60.0
cloudscraper>=1.2.71
pyppeteer>=1.0.2
yarl>=1.24.2

# DNS, Recon & Intelligence
dnspython>=2.8.0
python-whois>=0.9.6
tldextract>=5.3.1
wafw00f>=2.2.0

# Reporting / Cloud optional integrations
firebase-admin>=6.5.0

# Security tooling bridges
# Nuclei and WhatWeb are external CLI tools, not Python packages:
#   go install github.com/projectdiscovery/nuclei/v3/cmd/nuclei@latest
#   nuclei -update && nuclei -update-templates
#   gem install whatweb

# Test/verification
pytest>=9.0.2
